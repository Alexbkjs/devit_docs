# Backend Configuration
OPENAI_API_KEY=your_openai_api_key_here
SUPABASE_URL=your_supabase_url_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Documentation Sync Configuration (for sync-docs.js)
REPO_RAW_BASE=https://raw.githubusercontent.com/<org>/<repo>/main/
REPO_INDEX_URL=https://raw.githubusercontent.com/<org>/<repo>/main/scripts/docs_index.txt

# Mintlify Production URL (ALWAYS use production URL - stored in vector DB)
MINTLIFY_BASE_URL=https://devit-c039f40a.mintlify.app

# Local Development URL (optional - when set, URLs in API responses are transformed)
# Uncomment when running frontend locally to transform docs URLs to localhost
# LOCAL_DEV_URL=http://localhost:3000

# Server Configuration
PORT=9000
